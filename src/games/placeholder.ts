// Games placeholder - list of games to implement with full gameplay logic
// Each game module will export registerCommands(router) that the commands loader will import

export const plannedGames = [
  'tictactoe (complete moves + win detection)',
  'rps (tournament mode)',
  'hangman',
  'chess (integration or simplified)',
  'uno (turn-based multi-player)',
  'monopoly (skeleton for turns and properties)',
  'battleship',
  'sudoku/trivia pools'
];

// Example: export function registerAll() { /* register games in commands loader */ }
